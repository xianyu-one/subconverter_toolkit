# Subconverter 配置工具

本项目为订阅转换提供配置与前置代理能力。以下术语描述 Prefetch Proxy 管理私有节点的领域。

## Language

**私有节点**：由 Prefetch Proxy 管理、可按密钥选择并加入转换结果的代理节点。每个私有节点有唯一名称，可属于多个节点组。

**节点组**：私有节点的分类标签，同一节点可属于多个组。密钥可通过组引用其中的节点。

**密钥**：请求方使用的凭据，有管理名称和实际令牌。它可用于选择私有节点和订阅配置；私有节点的授权范围由节点名称与节点组共同指定，一个节点同时被多处引用时只选择一次。

**上游订阅**：由订阅服务商提供、作为转换输入的订阅地址。同一密钥下的不同订阅配置可引用同一上游订阅。

**订阅配置**：供一个密钥选择的一套上游订阅与转换参数。客户端使用稳定标识选择它；修改其上游订阅后，客户端无需更换订阅地址。

**可用上游**：在本次转换中提供了至少一个可解析节点的上游订阅。只有成功建立连接或取得 HTTP 响应，不足以使上游被视为可用。
