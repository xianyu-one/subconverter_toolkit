# 以 Mihomo 活动连接快照作为首版采集边界

Mihomo External Controller 的 `/connections` WebSocket 提供周期性的活动连接快照，而非完整的连接生命周期事件。首版 Observer 以此建立“已观测连接”历史，并记录采样间隔与采集缺口；不引入操作系统抓包。这个选择使部署和资源占用保持简单，但短连接可能漏采，连接消失不能证明精确结束时间或失败原因。分析和界面只呈现受这些限制支持的异常迹象，不将推断写成已证实的失败或 IPv6 回退。
