# 用单份 YAML 定义私有节点与多密钥授权

一个 Prefetch Proxy 实例以 `PRIVATE_CONFIG_PATH` 加载同一份 YAML 中的 `proxies` 和 `keys`：节点可用多个组标签分类，密钥通过节点名称和组选择节点，结果取并集并按节点定义顺序去重。直接替换单令牌与单节点文件的环境变量，不设置兼容期，因为工具尚未正式对外发布。相比为每个密钥复制节点清单，这个契约便于复用节点；代价是必须在启动时严格校验引用，且部署时需将节点资料与令牌作为同一份敏感配置管理。
