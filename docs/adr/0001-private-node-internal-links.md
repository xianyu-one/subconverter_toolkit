# 用短时授权链接向 Subconverter 提供私有节点

Prefetch Proxy 收到有效密钥后，向 Subconverter 提供短时有效、不可猜测的内部链接；链接只返回该密钥选中的私有节点，且不包含原始令牌。固定的 `/internal/private` 地址会向任何能访问服务端口的人暴露全部节点；把原始令牌放入内部链接又会增加凭据在 URL 日志中的暴露面。短时链接需要在有效期内保存选择结果，但能在现有单端口部署方式下限制直接读取与跨密钥读取。
